 <% @cosplays.each do |cosplay| %>
 <ul><%= cosplay.name %></ul>
 <%= link_to "Show More", cosplay_path(cosplay)%>
<% end %>
 <%# <% @cosplays.photos.each do |photo| %> %>
  <%# <%= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %> %>
<%# <% end %> %>
