<%# <% @cosplays.each do |cosplay| %>
 <%# <% @cosplay = Cosplay.find_by(cosplay: @cosplay) %>
 <%=@cosplay.name%>
<%# <% end %>

    <%# <% @list.movies.each do |movie| %>
      <%# <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
