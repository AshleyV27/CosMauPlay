<%# Welcome to booking page yoyo
<%= simple_form_for [@cosplay, @booking] do |f| %>
  <%# <%= f.input :starting_date %>
  <%# <%= f.input :ending_date %>
  <%# <%= f.submit "Submit booking", class: "btn btn-primary" %>
<%# <% end %>
<%= simple_form_for ([@cosplay, @booking]) do |f| %>
  <%= f.input :starting_date, as: :date, html5: true%>
  <%= f.input :ending_date, as: :date, html5: true%>
  <%= f.submit class: 'btn btn-flat' %>
<% end %>
